# README

The backend rails API
The controller will validate the csv file and send it to services/csv_parser to convert the CSV into a ruby object. Normally I would use a ruby gem to do this, but writing it out shows my looping ability.